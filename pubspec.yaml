name: united_ideas_flutter_test
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^3.1.0
  flutter_hooks: ^0.15.0
  hive: ^1.4.4+1
  hive_flutter: ^0.3.1
  json_serializable: ^3.3.0
  retrofit: ^1.3.4
  searchfield: ^0.3.2

dev_dependencies:
  build_runner: null
  hive_generator: null
  pedantic: null
  retrofit_generator: null

flutter:
  uses-material-design: true
  assets:
    - .env
